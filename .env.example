# Local MySQL Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=studentdb

# AWS S3 Configuration (Credentials are resolved via default AWS provider chain or IAM Role)
S3_BUCKET_NAME=your_bucket_name
# Optional: AWS_REGION=ap-south-1
